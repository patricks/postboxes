#!/usr/bin/env ruby

#lib = File.expand_path('../../lib', __FILE__)
#$LOAD_PATH.unshift(lib) unless $LOAD_PATH.include?(lib)
require 'postboxes'

importer = Postboxes::Importer.new
importer.import(ARGV[0])
